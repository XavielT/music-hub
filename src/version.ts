// The version this bundle was built from. Kept in step with package.json and
// the Android versionName/versionCode by `tools/set-version.sh` — bump with
// that script, never by hand.
export const APP_VERSION = '0.8.0';
