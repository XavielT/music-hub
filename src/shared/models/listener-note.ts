// One sentence, in one place, because it appears wherever someone would look
// for the button they do not have. Kept as a constant rather than typed twice:
// prompt 2 translates it, and a string that exists twice gets translated once.
export const LISTENER_NOTE =
  'Your account can listen and download everything in the library — ask Xaviel to add music to it.';
